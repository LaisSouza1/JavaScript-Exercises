const lasanhaPedido = true;
const refriPedido = false;

if (lasanhaPedido === true  && refriPedido == true) {
    console.log('Muito grata pelo almoço!');
} else {
    console.log('Acho que houve um engano com o meu pedido.');
}